import { LogoWrapper, LogoTitle, LogoSpan, LogoSignal } from './Logo.styled';

const Logo = () => {
  return (
    <LogoWrapper>
      <LogoTitle>
        ba<LogoSpan>zinger</LogoSpan>
      </LogoTitle>
      <LogoSignal />
    </LogoWrapper>
  );
};

export default Logo;
